import pandas as pdimport numpy as npimport matplotlib.pyplot as pltimport seaborn as snsdf_dates = pd.read_csv('~/Desktop/capstone/speeddating.csv')df_experiment = pd.read_csv('SpeedDatingData.csv', encoding='latin1')# Let's start by changing the empty spaces by nan values to have a clear image# of the datasetdf_dates.replace('?', np.nan, inplace=True)df_experiment.replace('?', np.nan, inplace=True)### Look into the datasets# df_dates.info(verbose=True)# df_experiment.info(verbose=True)equal = df_dates['age'] == df_experiment['age']print(equal)columns_df_dates = set(df_dates.columns)columns_df_experiment = set(df_experiment.columns)# Find common columnscommon_columns = columns_df_dates.intersection(columns_df_experiment)# Find columns unique to df1unique_to_df1 = columns_df_dates.difference(columns_df_experiment)# Find columns unique to df2unique_to_df2 = columns_df_experiment.difference(columns_df_dates)# Merge or concat datasets together to have a clearer vission of each #df = df_merged.applymap(convert_str)